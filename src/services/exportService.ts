import JSZip from 'jszip';

// This is the file you are looking at!
// Since we don't want infinite recursion in the zip, we won't include the giant data blob here.
export const downloadRepo = async () => {
    alert("This function handles the repo download!");
};